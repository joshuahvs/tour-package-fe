apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tour-package-fe-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: 2306165540-fe.hafizmuh.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tour-package-fe-service
                port:
                  number: 80